name: Deploy Frontend

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2

      - name: Install Dependencies
        run: npm install

      - name: Build
        run: npm run build
        
      - name: Connect Server
        run: echo "${{ secrets.KEY }}" > vps_github_temp
        
      - name: Test
        run: ls
        
      - name: Test2
        run: pwd

      - name: Deploy
        uses: appleboy/ssh-action@master
        with:
          host : ${{ secrets.HOST }}
          port: ${{ secrets.PORT }}
          username: ${{ secrets.USERNAME }}
          password: ${{ secrets.PASSWORD }}
          key: ${{ secrets.KEY }}
          script: |
            whoami
            pwd
            cd /var/www/gateways-portal/client/prod
            pwd
            rm -rf *
            scp -i ./vps_github_temp -r -p ./build/* ${{ secrets.USERNAME }}@${{ secrets.HOST }}:/var/www/gateways-portal/client/prod/
